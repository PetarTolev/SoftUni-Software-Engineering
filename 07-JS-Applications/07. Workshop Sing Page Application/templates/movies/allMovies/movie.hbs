<div class="movie">
    <h2>Title: {{movie.title}} ({{movie.year}})</h2>
    <img src="{{movie.image}}">
    <p>Available Tickets: {{movie.ticketsCount}}</p>
    <button><a href="#/buyTicket/{{movie.objectId}}">Buy Ticket</a></button>
    <button><a href="#/details/{{movie.objectId}}">Details</a></button>
</div>