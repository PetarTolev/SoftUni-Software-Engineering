<div class="container">
    {{> header}}
    {{#unless hasTeam}}
        <a href="#/create" class="btn btn-primary">Create Team</a>
    {{/unless}}
    <h1>Catalog Page</h1>
    <div class="teams">
        {{#each teams}}
            {{> team}}
        {{/each}}
    </div>
</div>

{{> footer}}